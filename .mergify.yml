pull_request_rules:
  - name: test-label
    conditions:
      - base=main
    actions:
      label:
        add:
          - mergify-works
      comment:
        message: Mergify rule evaluation works! 🎯