pull_request_rules:
  - name: auto-merge-test
    conditions:
      - base=main
      - label=merge-test-valid
    actions:
      merge:
        method: merge
        merge_bot_account: froggyfrog1234
