queue_rules:
  - name: default
    conditions:
      - base=main
    merge_conditions: []

pull_request_rules:
  - name: auto-merge-test
    conditions:
      - base=main
      - label=merge-test-valid
    actions:
      merge:
        method: merge
