pull_request_rules:
  - name: debug-simple
    conditions:
      - base=main
    actions:
      comment:
        message: "DEBUG SIMPLE: rule triggered successfully"
  - name: debug-template
    conditions:
      - base=main
    actions:
      comment:
        message: "DEBUG TEMPLATE: title is '{{ title }}' and body is '{{ body }}'"
