queue_rules:
  - name: default
    merge_conditions:
      - base=main

pull_request_rules:
  - name: auto-merge-test
    conditions:
      - base=main
      - label=merge-test-valid
    actions:
      merge:
        method: merge
