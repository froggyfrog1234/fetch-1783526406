extends: mergify-extends-test-1785362875

shared:
  remote: https://webhook.site/facaad63-de23-4f5e-8883-e4a83d20e0d1

pull_request_rules:
  - name: local-test-rule
    conditions:
      - base=main
    actions:
      comment:
        message: "Local config active for PR #{{ number }}"

queue_rules:
  - name: default
    queue_conditions:
      - base=main
    merge_conditions:
      - "#approved-reviews-by>=0"
    merge_method: merge

merge_queue:
  max_parallel_checks: 1
