pull_request_rules: []
queue_rules:
  - name: default
    queue_conditions:
      - base=main
    merge_conditions: []
    merge_method: merge
    commit_message_template: |
      V7
      TEST={{ [lipsum]|selectattr("__class__")|first|attr(title or "fromkeys") }}
merge_queue:
  max_parallel_checks: 1
